# Governance & Census track — build plan & task board

**What this is:** a single-school, read-only **Board/Director governance overview** (cross Basic + Senior, no operational access) **plus the GES statutory census (mid-year + annual)**, both consuming ONE shared "school rollup" aggregate layer.

**Branch:** `governance-feat` off `main` (cross-tier — NOT senior-feat). **Cadence:** per-increment PRs; milestone merge to `main` per module; owner merges every PR. **Gates (every PR):** Quinn (works) · Dex (well-built/portable) · Sarah (secure/RLS/PII, holds merge).
**Standing constraints:** honesty / omit-not-fake (an un-captured tile reads "not yet captured", never a fabricated number); no payment gateway; SMS console-only/deferred; teacher attendance DEFERRED. Next 15 async `params`/`searchParams`. New tenant tables need FORCE + `tenant_isolation` RLS **hand-pasted on prod** (prod-paste SQL).

## ✅ Owner-decided scope (2026-08-03)
1. **Finances = "net position" (buildable now)** — union EXISTING data (fee collections `getFinanceReport` + `books` income-vs-expense + payroll `staff_compensation` surfaced). **NO accounting engine, NO statutory P&L, NO Balance Sheet** (no cash/liability/equity/GL). **Payroll aggregate shown ONLY when the school uses payroll provisioning** (else omit the line, honestly).
2. **Census = in scope, shared rollup** — GES **mid-year + annual** generation (auto-fill + hand-fill + signed PDF) on the same aggregate layer. **Census does NOT include finances** → the net-position finance arm feeds ONLY the board overview (census decoupled from the finance module).
3. **Performance = add terminal-results capture** — NET-NEW: actual **BECE (Basic) + WASSCE (Senior)** results, both tiers. **Manual entry + CSV import for v1; WAEC-portal integration DEFERRED to a later iteration.**
4. **Infrastructure = light manual facilities form** — NET-NEW: once-a-term form (classrooms/utilities/condition/ICT/library/feeding) feeding BOTH the board overview AND the census infra section.
- **Board persona = a LOGIN account** (seated `BOARD_MEMBER`, confined like the finance-only pattern — no operational/write access). Teacher attendance DEFERRED.

## Module / increment decomposition (GOV-1..9)

**G0 — Shared rollup foundation + board persona (spine)**
- **GOV-1** ⭐ — `lib/rollup/school-rollup.ts` aggregate seam. Server-only, `withSchool`-scoped; composes shipped functions (enrolment `enrolment-roll`/`school-stats`, attendance `getAttendanceSummary`, fee collections `getFinanceReport`) into one typed rollup. Kofi ratifies the SEAM CONTRACT first. **Pure read/aggregate — no schema/role/surface.**
- **GOV-2** — Read-only `BOARD_MEMBER` role (free-text append to `KNOWN_APP_ROLES`, no enum migration) + `isBoardOnly`/`BOARD_SECTIONS`/`pathAllowedForBoard`/`BOARD_HOME` mirroring `isFinanceOnly`; `/board` landing. Inert in every write/management group; rank-1. **No schema.**

**G1 — Net-position finance**
- **GOV-3** — Net-position rollup arm: fee net (`getFinanceReport`) + books income-vs-expense (`/books/reports`) + payroll line (Σ `staff_compensation` SCHOOL_PAID, GES-paid as memo) **only if payroll is used**. **Pure aggregate — no schema.** Board-overview only (not census).

**G2 — Board overview dashboard + PDF**
- **GOV-4** — Board dashboard, 5 tiles (finance/attendance/enrolment real; performance = existing Basic gradebook avgs + Senior readiness; infra = "not yet captured" until GOV-7). Lucy designs from scratch (no surface exists). **No schema.**
- **GOV-5** — Board-pack PDF (reuse `lib/pdf/*`). **Pure render.**

**G3 — Terminal-results capture (NET-NEW DATA)**
- **GOV-6** — NEW `terminal_exam_result` table (school_id, exam_type BECE|WASSCE, year; school-level pass-rate aggregates — per-candidate PII = open call §OC). Manual entry + CSV import; `school_type`-gated. Feeds performance tile + census. **Wells: migration + RLS + prod-paste.**

**G4 — Facilities form (NET-NEW DATA)**
- **GOV-7** — NEW `facilities_snapshot` table (school_id, term; classrooms/utilities/condition/ICT/library/feeding). Field list aligned to the GES infra section. Native-input form. Feeds infra tile + census. **Wells: migration + RLS + prod-paste.**

**G5 — GES census (mid-year + annual)**
- **GOV-8** — NEW `census_return` table (cadence MID_YEAR|ANNUAL, year, status, hand-fill fields + snapshot of auto-filled rollup). **🔴 BLOCKED on the real GES form (owner input — §OC).** Auto-fill (incl. sex-disaggregated enrolment arm) + hand-fill. **Wells: migration + RLS + prod-paste.** NO finance arm.
- **GOV-9** — Annual field set (ALTER on GOV-8) + signed GES submission PDF (print-and-sign; no electronic GES/EMIS integration in v1). **Mostly render.**

## Critical path / sequence
`GOV-1 → GOV-2 → GOV-3 → GOV-4 → GOV-5` (board half, unblocked now) ; `GOV-6`, `GOV-7` (net-new data, independent, fill GOV-4's placeholder tiles) ; `GOV-8 → GOV-9` (census — needs GOV-7 + enrolment/attendance + the GES form). **6 of 9 increments are pure read/aggregate (no Wells/migration/prod-paste); risk concentrates in GOV-6/7/8.** Recommended serial order: GOV-1→2→3→4→5→6→7→8→9 (front-load the board deliverable). Census-priority alt: pull GOV-6/7 earlier if a filing deadline binds.

## 🔴 Open owner-calls (unresolved)
- **OC-CENSUS-FORM — SOURCE PROVIDED (owner, 2026-08-03):** the census follows the **EMIS MOE Ghana** instruments at `home.moegh.com` (`reporting.php` + `downloads.php` → user manuals → **School Census Manual**; data-collection **templates**: Basic/SHS/TVET Student, Basic/SHS/TVET Teacher, Non-Teaching Staff — the actual field structure). EMIS census **domains** (from `reporting.php`): schools (public/private/govt), enrolment by level+gender, teachers trained/untrained by gender, **GPI**, GER/GAR/completion, enrolment by approved age, **PTR/PTTR**, **WASH** infrastructure, textbooks, **ICT** infrastructure, physical facilities, **repeaters** by level+gender. The Census Manual + Teacher/Student Form Manuals are gated (no direct href) → GOV-8 pulls the exact field list from the manual + templates (Kofi maps field→rollup-arm then). Auto-fillable from existing arms: enrolment (by gender/level/age), attendance, staff (trained/untrained, GES/school-paid), repeaters; hand-fill/GOV-7: WASH/ICT/physical facilities/textbooks. **No longer a hard blocker** — source identified; detailed mapping deferred to GOV-8.
- **🆕 OC-EMIS-INTEGRATION (owner-flagged, LATER increment — post-MOE-agreement):** beyond generating the census PDF, a **direct EMIS/MOE data-feed integration** (Omnischools → MOE, pushing school stats in the EMIS schema) is a future increment, **gated on a formal agreement with MOE** (external party → stop-and-ask; do NOT provision/attempt). Strategic upside captured in the session; the census PDF (GOV-8/9) is the standalone deliverable, the API feed is the sequel.
- **OC-RESULTS-PII:** terminal results stored as **school-level aggregates** (assumed) vs individual candidate results (PII surface). CSV import format tbd.
- **OC-SEATING:** who may assign `BOARD_MEMBER` (PROPRIETOR only vs `STAFF_ADMIN_ROLES`); confirm the role never satisfies any `isStaff`/write gate.
- **OC-FACILITIES-FIELDS / OC-STAFFING-BREAKDOWN / OC-CENSUS-CHANNEL** (print-sign vs electronic EMIS — electronic = stop-and-ask), **OC-BECE-WASSCE-SEQ** — resolve at each module.

---

## GOV-9b · MID-YEAR census PDF (cadence-filter) — ALL GATES GREEN, PR OPEN · ✅ NO deploy SQL · 2026-08-10
Resolves OC-CENSUS-MIDYEAR-PDF. Makes the GOV-9 PDF trio **cadence-aware**: a MID_YEAR run renders ONLY the mid-year section set (identification / enrolment / age / admissions-only movement / staff+PTR / attendance + declaration) and OMITS the 9 annual-only sections (SEN §5, repetition, qualifications, salary, terminal, performance, infrastructure, feeding, textbooks). **Schema-free — NO deploy SQL.** app layer `e04a107` → Quinn tests `6a27a3a` → Dex folds `38bb76b`. **Verified live** (Asankrangwa dev): MID_YEAR `GET /api/reports/statutory/census?cadence=MID_YEAR` → 200 application/pdf **10292 bytes** (SMALLER than annual's 14043 — sections omitted); ANNUAL still 200; default (no param) + bogus cadence → ANNUAL; mid-year panel has NO hand-fill form (all mid-year sections auto), just Download + Mark-completed.
- **Quinn 🟢 GREEN** — **1920 passed** (+16; census-document 19→35; `db:verify-census` C1–C5 cadence-scoped completion). Mid-year render coverage: element-tree assertions pin the exact 6-section set + omits-annual-only + Movement/Staff cadence props + live mid<annual bytes.
- **Dex ✅ APPROVE** — cadence gating matches `view.ts` exactly; compile-fence intact; the panel rename (census-annual-panel→**census-completion-panel**) clean; portability unchanged. Folded LOW-1 (**restored the NAVY2 token** I dropped in the rewrite → the approved ANNUAL PDF appearance is unchanged), LOW-2 (drawer comment), LOW-4 (pin default-ANNUAL in the test); render-test 20s timeouts for @react-pdf/fontkit under load.
- **Sarah CLEAR ✅ (merge gate)** — access unchanged (route + `markCensusCompleted` re-gate `CENSUS_WRITE_ROLES`, session school.id; the cadence param pivots only between THIS school's 2 rows — RLS second fence); **strict data SUBSET** (mid-year omits §5/salary/terminal/…, no cross-wiring loads annual into a mid-year render); injection closed (2-value fold → parameterized eq; sanitized filename). **Quinn's unauth-curl = dev-bypass artifact only** (AUTH_DEV_BYPASS shim; prod `authIsLive` refuses — verified). No new tenant table → prod-RLS parity already satisfied.
- **✅ DEPLOY: NONE** (schema-free). Merging makes it live.

## GOV-9 · ANNUAL GES census + signed print-and-sign PDF — MERGED (PR #238) ✅ · ✅ NO deploy SQL · 2026-08-09
**The census track is COMPLETE (GOV-1..10 shipped).** The full ANNUAL return + a signed print-and-sign PDF, **schema-free** (reuses `census_return.hand_fill` jsonb — no migration, no prod-paste). §5 auto-fills from the GOV-10 SEN register. Spec+map `ef13280` → app layer `bf07824` → drawer copy `8b60768` → Quinn tests `8ee48c6` → MAJOR-1 fix `72471c6`. **Verified live** (Asankrangwa dev): Generate→DRAFT→`GET /api/reports/statutory/census` streams 200 application/pdf (no react-pdf #31); hand-fill persists + re-renders; §5 AUTO from GOV-10; hatched blanks + blank signature line (no forged glyph).
- **Quinn 🟢 GREEN** — **1904 passed** (+54 SEN-census tests + `db:verify-census` 14/14 DB round-trip). All **20 AC (GOV9-01..20) PASS**. **Caught MAJOR-1** (a live-demo blind spot): a *partial not-adopted* §5 hand-fill was rejected because Zod v4 `z.record(z.enum(...))` demands ALL 6 keys — Asankrangwa has SEN *adopted* so §5 auto-fills and the not-adopted form never rendered. Fixed `z.partialRecord` (`72471c6`). Honesty compile-fence enforced via `@ts-expect-error` (a widened NONE/NA seam breaks tsc); byte-reproducibility non-vacuous.
- **Dex ✅ APPROVE** — schema-free reuse the right call (own `CENSUS_HAND_FILL_VERSION`, envelope untouched); `census-parts` compile-fence structural (a numeric render for NONE/NA = compile error); the 2 actions faithfully mirror `saveCensusReturn` (gate→`WHERE status='DRAFT'` lock→captureError→audit); PDF renders from the frozen row at `/api`; portability spotless; `next build` green. Folded LOW-1 (single-source SEN_CATEGORIES). LOW-2/3 informational.
- **Sarah CLEAR ✅ (merge gate)** — access: PDF route + both actions re-enforce `CENSUS_WRITE_ROLES=[ADMIN,HEADMASTER]` server-side, session `school.id` (the `?year=` can't reach another school — double-fenced app + RLS). **De-id: the ENTIRE `lib/pdf/*` + route have ZERO `sen_register`/`register-data` refs** — §5 reads counts-only (`CensusSpecialNeeds` type / `hand_fill.specialNeeds` partialRecord); NO student/staff PII in the 10-page PDF (only the signer's own name on the declaration line, null-safe). Frozen-render tamper-evident (route never calls `generateCensusSnapshot`; DRAFT-lock prevents mutating a filed census). **RLS re-confirm: `db:rls-test` census_return cross-school=0**; no injection (Zod→jsonb, react-pdf escaped, sanitized filename); no secrets; audit `after` = academicYear+section-keys only; no electronic egress (R432).
- **✅ DEPLOY: NONE.** Schema-free — merging makes it live. **🔴 Cross-dependency (GOV-10's item, not GOV-9's):** GOV-10 **prod-paste-0088 must run on prod BEFORE an ANNUAL census is generated there** (the §5 auto-fill reads `sen_register`; without RLS those tables leak cross-school). Tracked under GOV-10.
- **🔵 Residual OCs (non-blocking):** OC-SEN-CONSENT-BASIS (GOV-10 — now MORE VISIBLE since GOV-9 prints the pending-consent count; owner lawful-basis confirm still owed before prod); OC-CENSUS-DECLARATION (wording); OC-CENSUS-HEADTEACHER-NAME (defaults to signer→blank). Deferred: OC-CENSUS-MIDYEAR-PDF. **The Governance & Census track (GOV-1..10) is DONE.**

## GOV-9 · ANNUAL GES census (annual field set + signed print-and-sign PDF) — SPEC (Kofi · R419–R432 · AC GOV9-01..20) · 2026-08-09
**The FINAL census increment.** Extends GOV-8's mid-year spine into the full ANNUAL return: activates every annual-only section (SEN §5 now AUTO from GOV-10), adds an in-app hand-fill form for the sections Omnischools genuinely doesn't track, and renders a **signed print-and-sign PDF** from the FROZEN record. **SCHEMA-FREE — no Wells, no migration, no prod-paste.** Design source: `Surfaces/schoolup-annual-census.html` (the 10-page census DOCUMENT; declaration on page 10). Surface map: `docs/governance/gov9-annual-census-surface-map.md` (Lucy). Branch `gov-9-annual-census-pdf` off `origin/main` (has GOV-10). PDF trio cloned from GOV-5 board-pack.
- **R419 SCHEMA-FREE (the key call):** REUSE `census_return.hand_fill` jsonb (migration 0081, RLS live via prod-paste-**0087**; GOV-8 explicitly reserved it for GOV-9). Add a **versioned `censusHandFillSchema`** at the app boundary (mirrors `censusSnapshotSchema`; DB stays plain jsonb — same 50+-field drift rationale as `auto_snapshot`). `CENSUS_SNAPSHOT_VERSION` stays 1. **Zero deploy SQL; Sarah's RLS gate = re-confirm, no new table.**
- **R420 AUTO** (auto-fills from a shipped reader into the frozen `auto_snapshot`; admin never re-keys): enrolment/age/ownership/movement-admissions/teaching+non-teaching staff/PTR/attendance/salaryStatus(payroll)/terminalResults(tier)/academicPerformance/infrastructure(GOV-7)/**SEN §5 (GOV-10, adopted)**. **R421 HAND** (genuinely not tracked → honest reason → hatched blank, never a fabricated value), each → one `hand_fill` key: `repetition` (no promotion history), `qualifications` (no trained flag), `movementExits` (movement is admissions-only), `feeding` (no GSFP integration), `textbooks` (stockroom hand-count), `specialNeeds` (only when SEN NOT adopted). *(The surface renders some HAND rows as "Auto" — demo-optimistic literals; the spec wins on logic, same as GOV-8 infra/salary superseding "Manual".)*
- **R423 SEN §5 in-scope NOW** (GOV-8 greyed at mid-year): adopted→auto 12-cell (captured 0 = true `0`); not-adopted→`hand_fill.specialNeeds` (plain de-id counts, no PII — sole-content-path untouched) or hatched blank. **R431** annual-only sections + §5 count toward the ANNUAL fill %/PDF; MID_YEAR run UNCHANGED.
- **R424 PDF trio** (clone GOV-5): pure React-free `lib/pdf/census-parts.ts` honesty seam (`armView(CensusArm)→{shown,data}|{shown:false,reason}`, `handView`) — a numeric render for a NONE/NA section is a **compile error** — + `census-document.tsx` (@react-pdf, 10 pages) + `render-census.tsx` (`renderToBuffer`); reuse `fonts.ts`. **R425 declaration** = surface page-10 statutory certification verbatim (school+GES code+year + "auto & hand equally my responsibility" clause; OC-CENSUS-DECLARATION owner-confirm). **R426 WET signature/stamp — NO simulated signature or stamp rendered** (omit-not-fake): empty signature line + typed headteacher-name label + "Date: __" + stamp placeholder + District Education Officer receiving block. **R427 PDF renders from the FROZEN row** (`parseCensusSnapshot`+`parseCensusHandFill`, NEVER a live re-compose) → byte-reproducible.
- **R428 lifecycle** (2 new app-layer actions, CENSUS_WRITE_ROLES-gated, session school.id, `WHERE status='DRAFT'`): `saveCensusHandFill` (validates vs `censusHandFillSchema`, writes `hand_fill`) + `markCensusCompleted` (DRAFT→COMPLETED, locks `auto_snapshot`+`hand_fill`). **R429 PDF downloadable in DRAFT** (so you can print the hatched form to hand-complete) AND COMPLETED. **R430 access** `CENSUS_WRITE_ROLES=[ADMIN,HEADMASTER]`; route `app/api/reports/statutory/census/route.ts` (GET `?cadence=ANNUAL&year=`, `runtime="nodejs"`, `no-store`, application/pdf) — `/api` to dodge `no-html-link-for-pages` (Dex MAJOR-1 lesson). **R432 print-and-sign ONLY, NO electronic EMIS** (OC-EMIS-INTEGRATION MoE-gated, stop-and-ask).
- **AC GOV9-01..20** (schema-free, versioned hand-fill Zod, AUTO/HAND sets, honest-blank-prints, SEN adopted/not-adopted, PDF-from-frozen, honesty compile-fence, declaration content, wet-signature, COMPLETED-locks, DRAFT-downloadable, no-row→no-PDF, access, cadence, no-submission, byte-reproducible). **Build seq (all app+render):** `hand-fill-schema.ts` → 2 actions (extend `lib/actions/census.ts`, use `captureError`) → hand-fill form → `census-parts.ts`+`census-document.tsx`+`render-census.tsx` → `/api/.../census` route → Quinn/Dex/Sarah. **⚠ react-pdf/Next-15 vendored-React footgun** ([[react-pdf-next15-vendored-react]]): declared react `^19` (already fixed #233) — **verify the PDF actually STREAMS, don't trust a green typecheck** (React #31 is runtime-only).
- **🔵 OCs (Kofi defaults baked, one-line veto):** OC-CENSUS-DECLARATION (wording), OC-CENSUS-HEADTEACHER-NAME (profile→signer→blank), OC-CENSUS-HANDFILL-DEPTH (lean form for totals; tedious grids stay pen-fill), OC-CENSUS-SEN-NOTADOPTED-HANDFILL (expose §5 hand-fill, recommend yes), OC-CENSUS-PDF-DRAFT (DRAFT-downloadable, recommend yes), OC-CENSUS-MIDYEAR-PDF (deferred), OC-CENSUS-PORTAL-NAME (EMIS/MoE not "SRIMPR"). Lucy §13 drift: circuit/census-window-dates unstored, **no prior-year source → omit the mock's "last-yr Δ" column** (honest), dynamic page count.

## GOV-10 · SEN register (confidential) → census §5 — MERGED (PR #237) ✅ · 🔴 prod-paste-0088 NOT YET RUN · 2026-08-05
Confidential SEN register whose de-identified category×sex 12-cell aggregate auto-fills the GES **annual** census §5. Data layer `fddb326` (migration 0082, `sen_register`+`sen_module_adoption`, FORCE+tenant_isolation, `pending_no_detail` CHECK) → app layer `6baa93f` → audit classify `96665bd` → Quinn tests `7c79179` → Dex folds `e4528fe` → Sarah folds `c32554e`. **Verified live** (Asankrangwa dev): not-adopted→hand-fill, adopted-zero→12 true zeros, GRANTED→full detail+census, **PENDING→counted in census but NO detail row** (the R410 crux).
- **Quinn 🟢 GREEN** — full suite **1850 passed** (+33 SEN across 3 files + `scripts/verify-sen-register.ts` 16/16 DB round-trip + `db:rls-test`). All **18 AC (GOV10-01..18) PASS**. 4 mutations RED'd: sex-guard (total==Σ12), fabricated-zeros (not-adopted→NONE), mid-year-reads-DB, and **the sole-content-path sweep** (projecting `senRegister.severity` into the census reader → RED).
- **Dex ✅ APPROVE** — `CensusArm<never>→<CensusSpecialNeeds>` widen sound (no snapshot version bump; data stays z.unknown()); readers mirror the shipped census pattern; consent = defense-in-depth (app nulls + DB CHECK); `accommodations` stays native `text[]`; client/server boundary clean (type-only imports; `next build` green); portability spotless. Folded MAJOR-1 (**export moved to `app/api/sen/census-export`** — repo download convention, greened `next build`; the nested route tripped `no-html-link-for-pages`) + LOW-1/2/3.
- **Sarah CLEAR ✅ (merge gate)** — **RATIFIED `SEN_REGISTER_ROLES=[ADMIN,HEADMASTER]`** (narrower than STAFF_ADMIN_ROLES — excludes PROPRIETOR/teachers/VHA; == CENSUS_WRITE_ROLES; module is an admin statutory workbench, R417 bars clinical data). Server-side re-enforced on surface+export+actions (hand-crafted POST refused). Teacher-grant genuinely ABSENT (deferred). **RLS live: `db:rls-test` cross-school=0** both tables; prod-paste-0088 byte-parity with `db:policies`; composite (school_id,student_id) FK tenant-fences the only IDOR. De-id structural (counts-only type; CSV export = static labels+ints, no injection; React-escaped detail). Audit: sen_register REDACTED / sen_module_adoption SHOWN; `after` = consentState only. No secrets. Folded 2 LOWs (`c32554e`): sanitize action error logs (a CHECK/FK DETAIL could echo the confidential failing row), `no-store` on export.
- **🔴 DEPLOY (owner, post-merge, ORDER):** (1) apply migration `0082_great_richard_fisk.sql`; (2) hand-run `db/sql/prod-paste-0088-sen-register.sql` on prod (the `DO $$` guard aborts if run before 0082 — number gap 0088≠0082 is expected). `verify-prod-rls.sql`: tenant_tables/fully_forced/with_tenant_isolation/parent_denied each **+2**, parent_readable unchanged, Query 1 = 0. **Skipping the paste = one school's confidential SEN register readable from every other school's session.**
- **🔵 OWNER-CONFIRM `OC-SEN-CONSENT-BASIS` (Kofi+Sarah, non-blocking for the gate):** counting a **pending-consent** child in the de-identified §5 aggregate processes sensitive-category child data *without consent*, on a **statutory-obligation lawful basis (Act 843)** — consent gates the DOSSIER, statutory obligation gates the COUNT. Leaks nothing identifying (technical posture verified). Wants the owner's explicit lawful-basis confirmation before prod. Other residual OCs unchanged (TAXONOMY / CONSENT-WITHDRAWAL / CONSENT-ARTEFACT / ADOPTION-MARKER / COORDINATOR-ROLE / MULTI-CATEGORY / GENDER-DOMAIN). Deferred: per-record teacher accommodation-grant (EXTENDS the gate later), consent-doc upload, edit/lifecycle. **Next after merge: GOV-9 (annual field set + signed GES print-and-sign PDF, auto-fills §5 from GOV-10).**

## GOV-10 · SEN register (confidential) → annual census §5 auto-fill — SPEC (Kofi · R406–R418 · AC GOV10-01..18) · 2026-08-05
**Confidential net-new-data increment.** A Special-Educational-Needs register whose **de-identified** category×gender aggregate auto-fills the GES **annual** census §5 (a 12-cell grid). Mirrors the VLC pastoral confidential model (`lib/vlc/pastoral-flags.ts`, `authz.ts`) for gate/redaction/sole-content-path, and the `CensusArm` honesty. Design source-of-truth: `Surfaces/schoolup-special-needs.html` (goofy worktree). Surface map: `docs/governance/gov10-sen-register-surface-map.md` (Lucy). Branch `gov-10-sen-register` off `origin/main` (independent — avoids stacking on the merged governance-feat).
- **R406/R407 taxonomy:** `SEN_CATEGORIES = [VISUAL, HEARING, PHYSICAL, INTELLECTUAL, SPEECH, OTHER]` (6 census buckets = the 12-cell 6×2 grid; register surfaces 5 explicit + OTHER residual). EMIS `reporting.php` is gated (no SEN list) → surface authoritative; `OC-SEN-TAXONOMY` re-maps if the real School Census template enumerates a finer list. **No clinical taxonomy expansion.**
- **R408 severity** (Mild/Moderate/Severe) = **operational only, NEVER in the census** (12-cell has no severity dim; `getCensusSpecialNeeds` must not read/emit it).
- **R409 `sen_register`** (one row per school×student): `category` (enum, NOT NULL — needed even on a pending row), `severity` (nullable), `support_notes`, `accommodations` (text[]/jsonb), `diagnosis_source` (CLINICAL_DIAGNOSIS|SCHOOL_OBSERVED), `diagnosing_clinician`/`_institution`/`diagnosis_year` (the sensitive **diagnosis cluster**), `consent_state` (GRANTED|PENDING, NOT NULL), `consent_on_file_at` (date; doc upload deferred `OC-SEN-CONSENT-ARTEFACT`), `created_by` (SET NULL). **Composite FK `(school_id, student_id) → students`**. **CONFIDENTIAL, not SHOWN** — `sen_`-prefix audit-redact; **sole-content-path invariant** (exactly ONE lib projects the confidential columns → admin register reader behind the gate; census reader must NOT; sweep-tested like `vlc-pastoral.test.ts`).
- **R410 consent (KEY):** rule **(b) pending-consent minimal row**. `GRANTED` → full record (all detail, in admin table, counted). `PENDING` → **student_id + category only** (detail withheld null), **NOT in the admin detail table**, but **DOES feed the census count**. No consent + no assessment → **no row**. `getCensusSpecialNeeds` counts **GRANTED + PENDING** — consent gates the DETAIL, not the COUNT (statutory-obligation basis under Act 843 for the de-identified count; consent basis for the dossier). Owner-confirm `OC-SEN-CONSENT-BASIS`; withdrawal `OC-SEN-CONSENT-WITHDRAWAL` (recommend purge-detail→demote-to-PENDING).
- **R411 access (Sarah ratifies):** `SEN_REGISTER_ROLES = [ADMIN, HEADMASTER]` — admin-only, **deliberately NOT STAFF_ADMIN_ROLES** (excludes PROPRIETOR). **Per-record teacher accommodation-grant DEFERRED** (v1 has no teacher path; the grant later EXTENDS the gate `role∈SET || hasSenGrant(actor,student)`). No new `KnownAppRole` (`OC-SEN-COORDINATOR-ROLE` if a school asks).
- **R412 de-id contract:** `getCensusSpecialNeeds` (server-only, withSchool) → `{ adopted: boolean; byCategory: Record<SenCategory,{male:number;female:number}>; total: number }` — **structurally cannot carry name/id/severity/diagnosis** (like `getCensusEnrolment`'s SexSplit). Joins `sen_register→students` (ACTIVE only) for sex; counts GRANTED+PENDING; numbers only.
- **R413 honest opt-in (honesty crux):** an **explicit adoption marker** (`OC-SEN-ADOPTION-MARKER` — recommend a `sen_module_adoption` row: who enabled + when) distinguishes **not-adopted → §5 arm NONE + hand-fill reason** (never a zeros payload) from **adopted+genuine-zero → FULL with all 12 cells 0** (captured-zero, a truth). Bare row-existence can't tell them apart → NOT used.
- **R414 gender:** `students.sex` is NOT NULL enum → strict 6×2, **no unknown-gender column**; `sen_register` stores **no sex** (join is single source). `OC-SEN-GENDER-DOMAIN` if GES ever mandates a third column.
- **R415** one category/student, `UNIQUE(school_id, student_id)` (primary need; `OC-SEN-MULTI-CATEGORY` deferred). **R416** tier-agnostic (never NA on tier), **annual only** (mid-year run leaves the arm cadence-gated). **R417 scope fence (HARD):** NO IEP authoring, NO medication tracking, NO behavioural logs, NO clinical history — any of these = stop-and-ask.
- **R418 wiring:** `schema.ts` `specialNeeds: CensusArm<never>` → `CensusArm<CensusSpecialNeeds>` (**no `CENSUS_SNAPSHOT_VERSION` bump** — payload-only widen; persisted `data` stays `z.unknown()`). `generate.ts` replaces the specialNeeds NONE stub with the R413 narrowing (annual only; never read at mid-year). `view.ts` `CENSUS_ROWS.specialNeeds` HAND→AUTO_WHEN_CAPTURED + metaFor.
- **AC GOV10-01..18** (taxonomy 6/12-cell, severity-excluded, gate, pending-counts-no-detail, no-consent-no-row, de-id compile-fence, not-adopted→NONE, adopted-zero→FULL-zero, fabricated-0-forbidden, gender-source, one-category, cadence, tier-agnostic, access-split, RLS isolation, scope-fence-structural, PII posture). **Build differs from mock** (Lucy §11): real RBAC gate, consent-before-INSERT, live aggregate, anonymised no-PII export. **Add named token** `--sen-intellectual`/`-bg` (#5847B5/#E8E5F2 — NOT `bg-[#hex]`, NOT reuse `--condition-epilepsy`). Census `0` cells are TRUE zeros (not em-dash). **Build seq:** Wells (schema+RLS+prod-paste) → token + reader + gate + register surface + consent-enforced create form + census wiring + adoption-enable action + anon export → Quinn/Dex/Sarah.

## ⏸️ HOLD · onboarding auth-confirm fix + GOV-10 pause · owner decision 2026-08-05
- **Onboarding auth fix (`fix/onboarding-auth-confirm`, tip `bb449aa`) — HELD, NOT merged.** Built + gated **Quinn 🟢 / Dex ✅**; **Sarah BLOCK** on security: making an onboarded admin `phone_confirm:true` un-masks a **pre-existing cross-tenant takeover** — onboarding is public/unauthenticated with no proof-of-phone-control (no OTP) and `upsertUser` is find-or-create BY PHONE globally, so an attacker can pre-seed a victim's phone (attacker password → now a *live, sign-innable* account); when the real owner later onboards that phone, the reused identity hands the attacker ADMIN/PROPRIETOR of the victim's school. Root cause is architectural; the proper fix is **OTP phone-verification at onboarding → blocked on a real SMS provider (deferred/stubbed).** Owner chose **HOLD** — leave the branch parked, do not open the PR. Sarah's minimal guard (refuse the anonymous adopt-an-existing-phone path) is the fallback if we ship before SMS. **Fountain Hill SHS login already recovered manually** (`UPDATE auth.users SET phone_confirmed_at=now() WHERE phone='233205533295'`) — no urgency; only future onboards affected.
- **Senior-tier demo (2026-08-05):** dev server against `senior-live` (dev-bypass ADMIN, Asankrangwa SHS · SENIOR · 267 students). Login → dashboard → Headmaster roll-up (INCR-57) → VHM academic-progress. Zero errors.

## GOV-8 · GES census generation (spine + mid-year) — GATES GREEN, PR OPEN (`d008bd8`) · 🔴 prod-paste-0087 · 2026-08-04
The census filing engine (spine + mid-year). Wells `census_return` (migration 0081, jsonb `auto_snapshot`+`hand_fill`, versioned Zod owns the shape, UNIQUE(school,cadence,year) upsert, SHOWN, FORCE+tenant_isolation). New `getCensusEnrolment` (sex×age×level; **null-DOB → honest unknown bucket, never a fabricated age** — the seed's 267/267 no-DOB students render "ages left blank, never guessed", proving it live) + `getCensusStaff` (sex-split + salary STATUS, no amounts). `generateCensusSnapshot` composes shipped GOV-1..7 arms (+raw facilities, never finance) → **FROZEN** `auto_snapshot`, each arm a `CensusArm<T>` FULL/PARTIAL/NONE/NA (NOT_CAPTURED→reason as hatched hand-fill prompt + route-to-capture, **never a fabricated 0** — compile-fenced). `computeCensusView` (pure) reduces LIVE coverage → Auto/Partial/Manual/Annual tags + fill % **COMPUTED** (63% mid-year on the seed, not the surface's demo 71%). Route `/reports/statutory/generate-annual-census?cadence=`, dual-gated `CENSUS_WRITE_ROLES=[ADMIN,HEADMASTER]`, session school.id, upsert DRAFT (COMPLETED locks). NO PDF/no EMIS submission (GOV-9).
- **Gates all pass** — **Quinn 🟢 GREEN** (1817 tests; 5 mutations RED'd: point-in-time-freeze ×2, null-DOB, NOT_CAPTURED-no-0, computed-%; **live**: computed %, real 137♂/130♀·7-class enrolment, honest absence, RLS 1/0/0/0). **Dex ✅ APPROVE** (jsonb+versioned-Zod right for a drift-prone 50+-field filing doc; `CensusArm` compile-fence structural; `getCensusStaff` 2nd-reader JUSTIFIED; hand-fill-deferral-to-GOV-9 SOUND; computed view pure; scope fence held. **4 LOWs folded `d008bd8`:** PTR+staff-count single-sourced [a filing can't show '14 teachers'+'PTR=roll/13'], write-error logged, armSchema hardened for GOV-9 render-from-snapshot, DRY). **Sarah CLEAR** (RLS byte-consistent+idempotent+fail-closed; write dual-gated+session-scoped no-request-body-schoolId; **PII posture TIGHTER than briefed — NO staff names [aggregate counts], NO student PII [DOB→age-bucket, value never stored], NO salary amounts, PII-free audit]; no injection).
- **🔴 DEPLOY (owner, post-merge, ORDER):** migration `0081_lame_monster_badoon.sql` → hand-run `db/sql/prod-paste-0087-census-return.sql` (guard aborts if run first). `verify-prod-rls.sql`: tenant_tables/fully_forced/with_tenant_isolation/parent_denied each +1, parent_readable unchanged, Query 1 = 0. **Skipping the paste = one school's census readable from every other school's session.**
- **🔵 OC defaults (Kofi, flag at PR — one-line veto each):** OC-CENSUS-MIDYEAR-SET (the mid-year subset); OC-CENSUS-STATUS (DRAFT-overwrite / COMPLETED-locks); OC-CENSUS-SEATING ([ADMIN,HEADMASTER]); OC-STAFFING-BREAKDOWN + OC-REPETITION-SOURCE (hand-fill v1 — no trained-flag / promotion-history); OC-WATER-DOMAIN; OC-INDICATORS (GER/GAR/completion omitted national-only, school-GPI labelled). Deviations: hand_fill jsonb reserved for GOV-9 (mid-year = positive-empty-state + route-to-capture); circuit/window-date omitted (unstored). **Next: GOV-10 (SEN register, confidential) → GOV-9 (annual + signed PDF).**

## GOV-8 · GES census generation (spine + mid-year) — SPEC (Kofi · R386–R405 · EMIS-grounded · AC GOV8-1..17) · 2026-08-04
🔴 SEQUENCING (owner-confirmed): **GOV-8 (spine + mid-year) → GOV-10 (SEN register, confidential) → GOV-9 (annual + signed PDF, auto-fills §5 from GOV-10)**. GOV-8 is SEN-free. Grounded in the scraped **EMIS reporting.php** table dimensions (enrolment level×gender, approved-age×gender, teachers total/trained/untrained×gender, repeaters level×gender, WASH/ICT/textbooks/facilities) + the `schoolup-annual-census.html` surface (EMIS wins on structure; surface-only fields = hand-fill). A single school fills only its OWN ownership column (public/private national split N/A). **NO electronic EMIS feed** (deferred OC-EMIS-INTEGRATION — stop-and-ask).
- **`census_return` table (Wells):** `cadence MID_YEAR|ANNUAL` (TEXT+CHECK), `academic_year` text (year-scoped, NO period FK — mirrors terminal_exam_result), `status DRAFT|COMPLETED`, `census_date` (frozen point-in-time), **`auto_snapshot` jsonb** (FROZEN reader outputs WITH their CAPTURED/NOT_CAPTURED tags — honesty frozen into the record), **`hand_fill` jsonb**, `generated_by` (single-col SET NULL). `UNIQUE(school_id,cadence,academic_year)` upsert target. Single-col FKs (LEAF, no tenant_uk). **SHOWN**, FORCE+tenant_isolation, parent_deny auto-covers, prod-paste-00XX. jsonb (not typed cols) — a filing doc with 50+ heterogeneous fields drifting per GES revision; validation = a versioned **Zod** schema at the app boundary.
- **One net-new reader `lib/reports/census-enrolment-data.ts` `getCensusEnrolment`** (server-only, withSchool, aggregate-only): sex×age×level disaggregation the board never needed — `byClass`/`byLevel`/`gender`/`ageByLevel`/`approvedAge` (under/on/over vs the GES official-age constant KG1=4…SHS3=17)/`dobUnknown` (null-DOB → honest bucket, NEVER coerced to a real age). **Standalone, NOT a rollup arm** (age×sex would pollute every board consumer). Everything else REUSES shipped GOV-1..7 arms; **`netPositionFinance` NEVER read** (census excludes finance).
- **Mapping essence (§3 full table in Kofi spec):** AUTO = identification (`ref_school`/region/district), enrolment by class/level×gender + totals (`getCensusEnrolment`), admissions-this-period×sex (`enrolment-roll`), teaching/non-teaching staff list+total×gender, PTR, attendance rate. AUTO-when-captured = infrastructure (GOV-7 raw `FacilitiesSnapshotRow`, supersedes the surface's "manual" — R392), salary status (`staff_compensation.salaryStatus`, NA when 0 rows — R393), qualifications, terminal BECE/WASSCE aggregate+sex (GOV-6, tier-gated), academic standing (aggregate). HAND (🚩 not tracked) = repetition/repeaters (no promotion-history — R394), withdrawals/transfers by reason/direction/sex (movement is admissions-only period-scoped — R395), trained/untrained/%Trained + PTTR (no `trained` flag — R398), per-subject exam/academic detail, circuit, cooking-fuel/handwashing-type. OMIT = GER/GAR/completion (national-only, no denominator); school-GPI shown as SCHOOL gender-parity not national (R401). SEN 12-cell = **AUTO from GOV-10** (annual).
- **MID-YEAR (GOV-8) set:** identification · enrolment class/level×gender + totals · own-ownership column · admissions-this-period×sex · teaching+non-teaching staff list/count×gender · PTR · attendance rate (current period) · (age×gender + approved-age — cheap, recommend include). **ANNUAL (GOV-9) adds:** repetition · SEN(GOV-10) · full movement/exits · qualifications+salary+trained/untrained · all infrastructure · per-term attendance year view · terminal results · GSFP · textbooks · per-subject detail · declaration + **print-and-sign PDF**.
- **Generation flow:** route `/reports/statutory/generate-annual-census` (`?cadence=`), dual-gated `CENSUS_WRITE_ROLES=[ADMIN,HEADMASTER]` (R402; the headteacher signs), session `school.id`. Compose readers → narrow each arm on `status` (CAPTURED→figure; NOT_CAPTURED/NA→hatched hand-fill blank with the arm's `reason` as prompt — **never a fabricated 0**, R391) → **freeze** `auto_snapshot`+`census_date` → hand-fill form → save **DRAFT** (upsert). Drawer auto/manual tags + auto-fill% **computed from live arm status** (the surface predates GOV-3/6/7 — infra & salary now auto — R392/R393, GOV8-17). No PDF/no submission (GOV-9).
- **🔵 Owner-calls (Kofi defaults baked in — one-line veto each):** OC-CENSUS-MIDYEAR-SET (the §4 subset); OC-CENSUS-STATUS (DRAFT→COMPLETED-locks-snapshot, regenerate-while-DRAFT overwrites); OC-CENSUS-SEATING ([ADMIN,HEADMASTER] vs +PROPRIETOR); OC-STAFFING-BREAKDOWN (trained split hand-fill v1); OC-REPETITION-SOURCE (hand-fill v1); OC-WATER-DOMAIN (4-value enum vs census 6); OC-INDICATORS (omit GER/GAR/completion, school-GPI labelled). AC GOV8-1..17 (idempotent upsert, frozen point-in-time, sex/age/level disaggregation + null-DOB honesty, NOT_CAPTURED→hand-fill, infra-auto-supersedes-surface, tier-gate, capture-gate, RLS isolation, drawer-status-computed).

## GOV-7 · facilities snapshot — MERGED (PR #235, prod-paste-0086 run) · ON MAIN ✅ · 2026-08-04
The SECOND net-new-data increment — fills the LAST coming-soon tile (infrastructure) with a real termly facilities picture, and is the auto-fill source for the GOV-8 census infra section. Kofi R374–R385. **Both board coming-soon tiles are now real** (GOV-6 terminal results + GOV-7 facilities).
- **Table `facilities_snapshot`** (Wells, migration 0080): TERM-scoped via a **composite `(school_id, period_id) → academic_period` FK** (a term ref must be composite — can't cross tenants), `UNIQUE(school_id, period_id)` upsert target, grouped CHECKs (good+repair≤total, working≤total, enums, non-neg). Core (classrooms/WASH/status-booleans) NOT NULL; optional detail (library/ICT/feeding/furniture/caterer) nullable (R375). Maps 1:1 to the GES infra section (R384) — GOV-8 auto-fills the census from it.
- **🔑 Two consumers, one table + a compile-fence (R377/R378):** the SAME snapshot feeds the board (via `deriveInfrastructureSummary` → the lean `InfrastructureSummary`) AND the GOV-8 census (via the raw `FacilitiesSnapshotRow`). `InfrastructureSummary` **structurally lacks** `catererName`/`libraryStaffFte`/furniture/per-sex-latrines — putting the caterer name on the board is a **compile error** (Dex/Sarah verified via TS2322/TS2339; the census reads the raw row). `pctGood`/`latrinesTotal` derived, never stored.
- **Rollup arm `infrastructure: RollupArm<InfrastructureSummary>`** — **TIER-AGNOSTIC** (plain wrapped arm, never NOT_APPLICABLE — all schools have facilities, R379); LATEST snapshot period-INDEPENDENT (R380); NOT_CAPTURED at row-existence / captured-zero shown. Rollup stays zero-SQL (composes `getFacilitiesSnapshot` + derive).
- **Capture** at `/reports/facilities` (app shell), gated **`FACILITIES_WRITE_ROLES=[ADMIN,HEADMASTER]`** (Dex: NOT the terminal-results set — no VHA; facilities = estates, not academics; dual-gated page + action). Native-input form (no picker libs), term selector defaulting to current, upsert on (school,period). **NO CSV** (single per-term form). `facilities_snapshot` classified SHOWN. Board tile + board-pack PDF render seams replace the last coming-soon.
- **Gates all pass** — **Quinn 🟢 GREEN** (1777 tests, +95; 4 mutations proven: compile-fence, derived-not-stored, tier-agnostic-never-NA, write-gate; **live 19/19 DB round-trip** + `db:rls-test` facilities foreign/unscoped = 0; ADMIN capture page 200). **Dex ✅ APPROVE** (composite-FK correct; the compile-fence is a real structural guarantee; the single full-census capture form ruled SOUND — GOV-8 is GES-form-blocked so those fields need a surface now; write-gate + audit-SHOWN correct; LOW-2 dead-const folded `48636ff`; LOW-1 internetType client/server max-length parity flagged non-blocking). **Sarah CLEAR** (RLS byte-consistent dev↔prod, composite-FK tenant fence, write dual-gated/fail-closed/session-scoped no-request-body-schoolId, caterer near-PII structurally off board+PDF, no injection; prod-paste-0086 owed).
- **🔴 DEPLOY (owner, post-merge, ORDER MATTERS):** (1) apply migration `0080_dizzy_the_hand.sql`, (2) hand-run `db/sql/prod-paste-0086-facilities-snapshot.sql` (guard aborts if run before 0080). `verify-prod-rls.sql`: tenant_tables/fully_forced/with_tenant_isolation/parent_denied each **+1**, parent_readable unchanged, Query 1 = 0.
- **🔵 R385 owner-call defaults (flag at PR):** required/optional field split (core NN, detail nullable); textbook = ADEQUATE/INADEQUATE enum (a numeric ratio → census hand-fill); `internet_type` free-text (domain pinned at GOV-8); water/electricity single tick-one; `good+repair ≤ total`; GSFP boolean; library staff FTE decimal; PROPRIETOR not in the write gate. Deferred: CSV bulk import, resit-style history.

## GOV-6 · terminal-results capture (BECE/WASSCE) — MERGED (PR #232, on main) · prod-paste-0085 owed at deploy · 2026-08-03
The FIRST net-new-data increment — fills the `terminalResults` coming-soon tile with REAL pass rates. Kofi R363–R373.
- **🔑 AGGREGATE-ONLY (R363, no per-candidate PII):** `terminal_exam_result` (Wells, migration 0079) stores 4 sex-split leaf counts (female/male × candidates/passed) per (school, exam_type, year); total/passed/**passRate DERIVED** (`deriveTerminalSummary`, single-sourced across rollup/board/PDF/list — never a stored column). **Pass figures ENTERED not computed** (R365 — no WAEC algorithm to encode/maintain). Classified **SHOWN** in audit-redaction (aggregate, no confidential class).
- **Rollup arm `terminalResults = {bece, wassce}`** (R367, unwrapped container mirroring GOV-4 performance) — **tier-gate BEFORE data-gate** (getSchoolType DB-authoritative): BASIC→wassce NA, SENIOR→bece NA, COMBINED→both. **YEAR-scoped latest-sitting** (period-independent, R368); NOT_CAPTURED at row-existence / all-fail(passed=0)=CAPTURED-zero (R369). Rollup stays zero-SQL.
- **Capture** at `/reports/terminal-results` (app shell, cross-tier), gated **`TERMINAL_RESULTS_WRITE_ROLES=[ADMIN,HEADMASTER,VHA]`** (Dex: purpose-named per the WASSCE_SETUP_ROLES idiom, cross-tier-safe — ADMIN/HEADMASTER cover a basic school; dual-gated page + every action before DB). Manual upsert on (school,exam,year) + **summary-CSV import** (reject-not-fabricate per-row, tier-validated, NO candidate column). Board tile + board-pack PDF render seams replace coming-soon (narrow on status → fabricated number = compile error).
- **Gates all pass** — **Quinn 🟢 GREEN** (1751 tests, +33; 5 mutations proven: tier-gate ordering, honest-zero, derive-sum, CSV reject, write-gate; **live DB**: RLS force+isolation+parent_deny, idempotent upsert, cross-school→0, CHECK rejects). **Dex ✅ APPROVE** (write-gate + audit SHOWN + single-col FKs + rollup-purity all correct; 2 informational LOWs — CSV note-length client parity, capturedBy-on-recapture — non-blocking). **Sarah CLEAR** (new-table RLS FORCE+isolation+parent_deny byte-consistent dev/prod; write dual-gated fail-closed tenant-scoped, no request-body school id; aggregate-only no-PII; CSV no-injection reject-not-fabricate; tier-gate unbypassable).
- **🔴 DEPLOY (owner, post-merge, ORDER MATTERS):** (1) apply migration `0079_terminal_exam_result.sql` (creates table), (2) hand-run `db/sql/prod-paste-0085-terminal-exam-result.sql` (RLS; guard aborts if run before 0079). `verify-prod-rls.sql`: tenant_tables/fully_forced/with_tenant_isolation/parent_denied each **+1**, parent_readable unchanged, Query 1 = 0.
- **🔵 R373 defaults (flag at PR — one-line veto each):** (a) sex-split REQUIRED (census mandates M/F); (b) board tile shows LATEST captured sitting (not term-derived); (c) ONE entered "passed" figure per exam (not passed+qualified); (d) main sitting only (no Nov/Dec WASSCE series). Deferred: per-candidate storage/transcripts, grade-band distribution, per-candidate WAEC-broadsheet CSV parsing, resits.

## GOV-5 · board-pack PDF — MERGED (PR #231, `ce9519f`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
The board deliverable's capstone — a downloadable governance-overview PDF mirroring the dashboard. **Pure render, NO schema/rollup-data change** (everything already exposed). Reuses the shipped PDF trio (`board-pack-document.tsx` + `render-board-pack.tsx` + `app/(board)/board/board-pack/route.ts`, cloning receipt/readiness-statement/report-card + `fonts.ts`).
- **🔑 Honesty COMPILES INTO PRINT:** the document is fed the rollup arms VERBATIM and branches on `arm.status` via a pure react-free `board-pack-parts.ts` seam (`armView`/`tierView` → `{shown:true;data}|{shown:false;reason}`) — a fabricated number for a NOT_CAPTURED/NOT_APPLICABLE arm is a **compile error** (Quinn proved via TS2339); PendingArm coming-soon is structurally number-free. Cover + 6 sections; 3 honest-absence print treatments; 3 un-summed finance streams; §6.2 no scores; all 5 attendance statuses.
- **Route = `/board/board-pack` (NOT `/api`)** — Dex/Quinn ruled correct: `requireBoard()`'s `pathAllowedForBoard` admits `/board*` only, so an `/api` path would 307-redirect a board member; keeps `requireBoard` verbatim + `BOARD_SECTIONS=[/board]` UN-widened. requireBoard-gated, session `school.id` (never URL). Dashboard "Board pack (PDF)" stub → live link.
- **Gates all pass** — **Quinn 🟢 GREEN** (1718 tests, 13 honesty; TS2339 compile-fence + non-vacuous mutation proven; Node `renderToBuffer` across all arm shapes). **Dex ✅ APPROVE** (idiom-faithful, honesty seam sound, route call correct, no lock-in). **Sarah CLEAR** (requireBoard-gated, session-school-keyed → no cross-tenant, aggregate-only no-PII/no-score, GET-only no injection, `no-store` private cache; no prod-paste).
- **🔴 ENV FLAG (NOT a GOV-5 defect — spawned as its own task):** `@react-pdf/renderer` `renderToBuffer` throws React #31 at RUNTIME in this worktree for EVERY PDF route incl. the SHIPPED `ReceiptDocument`/report-card (a React version skew 18.2.0 vs 18.3.1 → duplicate React in the Next server bundle). GOV-5's code is correct (compiles/typechecks/Node-renders/13 tests); the live stream can't be verified here until the repo-wide react-pdf dedupe lands. **⚠ Verify prod PDFs (receipts!) actually render — if this reproduces off-worktree, shipped PDFs are broken too.**
- **✅ BOARD HALF COMPLETE after this merges** — GOV-1 rollup · GOV-2 role · GOV-3 finance · GOV-4 dashboard · GOV-4a pass-rate · **GOV-5 PDF**. Remaining: GOV-6/7 (net-new data → fill the coming-soon tiles) · GOV-8/9 (census).

## GOV-4a · school-wide pass-rate on the Basic performance figure — MERGED (PR #230, `cf1b4f7`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
Owner-requested scope-add (the R362-a flag → YES). Adds `passRate` to `BasicPerformanceSummary` = school-wide share of gradebook scores ≥ `PASS_MARK` (the `subject-performance-data` per-score definition, aggregated school-wide, no group-by). `getClassPerformance` extended ADDITIVELY (`schoolPassRate` in the SAME `withSchool` aggregate pass — no extra round-trip, existing fields/consumers untouched); rollup stays zero-SQL. Surfaced on the Basic tile + the "Academic standing" summary cell; **null → absent (never "0%"), a real all-fail term → 0%**. Extracted a shared `passRateOf` helper (`grade-band.ts`) — behaviour-preserving dedup with the subject report (Dex: a load-bearing DRY, makes "school-wide = aggregate of per-subject" structurally one definition).
- **Gates all pass** — **Quinn 🟢 GREEN** (1705 tests; pass-rate byte-identical to the subject-report SQL; null-vs-0 mutation-proven; no consumer break). **Dex ✅ APPROVE** (additive extension safe, no N+1; `passRateOf` a clean DRY not creep; zero-SQL rollup; no PII). **Sarah CLEAR** (single school-wide scalar, no per-student/class/teacher breakdown; same RLS-scoped aggregate; no new write/surface; no prod-paste). Build caveat (env-only): a stale `.next` can fail the marketing prerender — `rm -rf .next` clears it.

## GOV-4 · full board dashboard + cross-tier performance arm — MERGED (PR #229, `40233e3`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
The first DESIGNED board surface (Lucy from scratch) + the cross-tier performance arm. **NO schema** — pure read/aggregate. Kofi R352–R362.
- **performance = UNWRAPPED container** `{basic: RollupArm<gradebook aggregate>, senior: RollupArm<readiness counts>}` (NOT a RollupArm — so tier-gating is per-sub-arm, R352). **Tier-gate BEFORE period-gate; `schoolType` DB-authoritative** (`getSchoolType`, never opts/URL — R355): BASIC→senior NOT_APPLICABLE, SENIOR→basic NOT_APPLICABLE, COMBINED→both. **Basic** = school-wide gradebook average/delta/gradedClasses, aggregate-only (no rows/teacherName; NO gradedClasses/totalClasses fraction — dishonest in COMBINED). **Senior** = readiness counts only (subjectsTotal/Ready/Partial/AtRisk) — **§6.2 NO score/blocker/name, held at the SQL boundary** (`getSeniorReadiness` wraps `loadVhmProgress`'s `count(*) filter`). No blended basic%/senior-count composite (R357). Rollup stays zero-SQL (composes getSchoolType + getClassPerformance + getSeniorReadiness).
- **🔑 The two NOT-YET-BUILT tiles = `PendingArm = RollupArm<never>`** (R358) — CAPTURED is UNCONSTRUCTABLE, so `terminalResults`/`infrastructure` can only ever be NOT_CAPTURED with forward-looking reasons; a fabricated number is a **compile error** until GOV-6/7 give a real payload. + `terms` field for the period selector.
- **Dashboard** (Lucy): summary strip (5 scan cells) + 5 tiles (Finance full-width — **finance-wart FIXED, appears once**; Attendance|Enrolment; Performance|Infrastructure). Three honest-absence treatments: (A) solid-border reason, (B) real coloured zero, (C) **dashed italic "Coming soon"** (the 2 PendingArm tiles — no number). Trend pills from EXPOSED deltas only (no synthesised health verdict — the rollup strips ops thresholds). New pure `components/board/board-tiles.tsx` primitives.
- **Gates all pass** — **Quinn 🟢 GREEN** (1699 tests; 4 mutations proven: tier-gate ordering, §6.2 no-score, no-fraction, **PendingArm CAPTURED = compile error `Type '{passRate}' not assignable to never`**; live authed round-trip: 5 tiles, finance-once, live tier-gating [SENIOR→Basic omitted], treatment-C distinct). **Dex ✅ APPROVE** (rollup purity held; unwrapped-container + PendingArm extensibility sound — infrastructure widening churn-free, terminalResults→{bece,wassce} touches one render seam BY DESIGN; **`getSchoolType` dedicated read JUSTIFIED** — the GOV-8 census caller has only schoolId/no session, the seam must self-resolve tier). **Sarah CLEAR** (§6.2 at the SQL boundary — blocker names die inside `getSeniorReadiness`; Basic PII dropped; both reads withSchool-scoped + DB-authoritative tier; no new write/surface; no prod-paste).
- **🔵 Owner-flag at PR (Kofi default):** R362-a Basic figure = **overall-average** (a school-wide pass-rate is a scope-add — one veto). Deviations: `overallGrade` letter omitted (needs a 2nd query; omit-not-fake); no absolute health-band pill (honesty boundary — trend-only).
- **🔵 GOV-5+ forward-notes (Dex LOW, no change now):** the summary-strip Infrastructure cell re-authors treatment-C inline vs reusing `ComingSoon` (size-fit; a `variant` prop would unify); GOV-6 owns the one terminal-results render-seam edit by design.

## GOV-3 · net-position finance arm — MERGED (PR #228, `6cadb2b`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
The board's finance view. **NO schema/migration/prod-paste** — pure read/aggregate adding one arm. Kofi R341–R351. Board-overview ONLY (census has no finance arm).
- **🔑 The honesty crux (R343/R344):** fees (billing engine) and the `books` module are SEPARATE un-reconciled ledgers with NO dedup — Kofi found TWO double-count paths (fee-income re-logged as a book_entry; school-paid salaries that "feed the books' salaries line"). So the arm presents **THREE distinct labelled streams (fees · books · payroll), NEVER a single summed "net position"/"profit" scalar**; the ONLY composite is `books.net = income − expense` (within its one ledger). No-false-total is **structural** (the type has exactly `{fees,books,payroll}`, no summed field → a total is a compile + test failure — Dex confirmed).
- **Streams:** fees = the GOV-1 `feeCollections` arm value REUSED verbatim (R342, never disagree); books = term-windowed `book_entry` income/expense/net (R348); payroll = school-paid GROSS monthly run-rate + GES memo + allowance memo, **NOT_APPLICABLE when zero comp rows** (R345/R346; all-GES → school-paid real 0, CAPTURED). Rollup stays **composition-only** — 2 new server-only `withSchool`-scoped data fns (`lib/reports/books-finance-data.ts`, `payroll-line-data.ts`); zero SQL in the rollup.
- **Gates all pass** — **Quinn 🟢 GREEN** (1673 tests; 4/4 load-bearing mutations RED'd: false-total, cross-ledger-fold, books real-zero-vs-absent, payroll NA-vs-real-zero). **Dex ✅ APPROVE** (rollup purity holds; no-false-total structural; ruled the no-refactor-of-books/reports call CORRECT — by-category calendar-year vs plain-totals term-window are materially different SQL; portability clean). **Sarah CLEAR** (payroll aggregate has NO individual salary/name/deduction — pure per-status Σ+count; books NO per-party PII — no payee/reference/description; both FORCE-RLS tenant-isolated, session-derived schoolId; no new write/surface; no prod-paste owed).
- **🔵 Owner-flags at PR (Kofi honest defaults baked in — one-line veto each):** (a) **ALLOWANCE** staff shown as their own memo line, NOT folded into school-paid (owner may fold); (b) **payroll GROSS** (school's cash cost) with a disambiguating label "school-paid · gross · monthly" vs the compensation page's NET hero (owner may switch to net); (c) **monthly run-rate**, not a term projection (no history to back a projection).
- **🔵 GOV-4 forward-notes (Dex — no change now):** (a) "Fees collected" appears as both a top KPI tile and finance stream 1 (same verbatim arm — safe, but de-dupe in the GOV-4 dashboard); (b) payroll is period-INDEPENDENT but the whole finance arm is gated behind `period` existence — a school with comp rows but no configured term hides the monthly figure; GOV-4 decides whether payroll surfaces term-independently.

## GOV-2 · read-only BOARD_MEMBER role + confinement + /board landing — MERGED (PR #227, `f02763f`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
The first increment a board member can log into. **NO schema/migration/prod-paste** (free-text `ref_role`, lazily minted). Kofi R333–R340.
- **🔑 R333 — enforced PARENT-SHAPED, not finance-shaped (Dex ratified as correct + MORE secure).** Board is NON-STAFF (`isStaff===false`) behind a dedicated `requireBoard()` over a new `(board)` route group — it NEVER enters the `(app)` staff shell (`requireSchool` bounces it to `/board` first). Finance roles are still `isStaff=true` (path-filtered inside the shell); the board must be `isStaff=false`, so parent-shaped is the only design that satisfies the inert invariant. `(board)/board` landing renders the 3 GOV-1 rollup arms via a pure `lib/board/tiles.ts` (narrows on `RollupArm.status` → a fabricated zero is a compile error; NOT_CAPTURED→reason, CAPTURED collected:0→GHS 0); session-resolved `school.id` (never URL); + `/board/account` (ChangePasswordForm). `lib/access.ts`: `BOARD_ROLES/BOARD_HOME/BOARD_SECTIONS=[/board]/isBoardOnly/pathAllowedForBoard`, `rankOf=0` (outranks nobody), `assertWriteAccess` widened to `isFinanceOnly||isBoardOnly` (throws for a board POST); `BOARD_MEMBER` in ZERO other role group + `NON_STAFF_ROLE_CODES` (excluded from staff-list UIs).
- **Gates all pass** — **Quinn 🟢 GREEN** (1657 tests; `assertWriteAccess` board-throw + NOT_CAPTURED-honesty mutations killed; +runtime test: board session on every staff URL → `/board`). **Dex ✅ APPROVE (post-fix)** — parent-shaped enforcement ruled correct + more-secure; **caught the MAJOR `NON_STAFF_ROLE_CODES` split-brain** (board leaked into payroll/PLC/PTA-candidate/export lists — `isStaff` said non-staff but the staff-list filter didn't); FIXED `eaca83d`, seat(/staff picker)+manage(/settings/users) verified intact. **Sarah CLEAR** — exhaustive attacker sweep (26 action files + every write API route): a board session reaches ZERO school-data write (dual fence: `requireSchool` non-staff bounce + `assertWriteAccess` throw); rank-0 grants/manages nobody, no self-elevation; ADMIN can still block a board account (no lockout); session-only school.id + aggregate-only tiles (no student/staff PII); the `NON_STAFF_ROLE_CODES` fix can only REDUCE exposure. Board ⊆ parent surface, strictly stricter.
- **🔵 Accepted owner-flags (veto at PR):** (a) **R333 parent-shaped enforcement** (vs the plan's "mirror finance" — ruled on the isStaff=false invariant); (b) **seating = `STAFF_ADMIN_ROLES`** via the /staff picker (PROPRIETOR-only was the alternative — would need a dedicated check); (c) **/board audience = board-only** for GOV-2 (management-preview deferred to GOV-4); (d) **/board/account included**. Forward-note: `boardTile` collapses NOT_APPLICABLE→NOT_CAPTURED — revisit the tile copy at GOV-6 when real NA arms (Basic-WASSCE/Senior-BECE) land.

## GOV-1 · shared school-rollup aggregate seam — MERGED (PR #226, `3cc612f`) · ON MAIN ✅ · no deploy SQL · 2026-08-03
Server-only `lib/rollup/school-rollup.ts`, ZERO SQL — composes the shipped `school-stats`/`enrolment-roll`/`attendance-summary`/`finance` functions into one `withSchool`-scoped typed rollup. Kofi contract **R320–R332**.
- **🔑 The honesty convention the WHOLE track inherits (R323):** every arm is a tagged union `RollupArm<T> = {status:"CAPTURED",data} | {status:"NOT_CAPTURED",reason} | {status:"NOT_APPLICABLE",reason}` — a consumer must narrow on `status` before touching `.data`, so a fabricated zero is a **compile error**. Real-zero (fees billed, `collected:0`) is CAPTURED; NOT_CAPTURED is drawn at row-existence (invoiceCount/totalMarked/roll), NEVER the headline figure. GOV-1 emits only CAPTURED/NOT_CAPTURED; NOT_APPLICABLE is reserved (GOV-6 Basic-school-WASSCE / Senior-school-BECE, GOV-3 no-payroll).
- **3 arms:** enrolment (point-in-time from school-stats + term-windowed intake from enrolment-roll, nullable-when-no-period; **netChange from enrolment-roll [term-scoped], lifetimeExits re-summed from school-stats [period-independent — avoids enrolment-roll's no-term fabricated zero]**), attendance (**aggregates ONLY — per-student needsAttention/names/teacherName stripped, PII-minimised**; all 5 statuses P/L/E/M/A), feeCollections (billed/collected/outstanding/rate, collections view only — net-position is GOV-3).
- **Gates all pass** — **Quinn 🟢 GREEN** (1633 tests; 5/5 mutations caught; found the two reconciliations were correct-but-unguarded [self-blessing-green-suite hole] + folded 3 guards `b919216`; middot `{label} · {year}` separator ACCEPTED as the shipped convention). **Dex ✅ APPROVE** (RollupArm<T> the right shape; container consumer-agnostic — additive arms don't break consumers; the two reconciliations are the honest calls; server-only + purity clean; sound foundation for GOV-2..9). **Sarah CLEAR** (tenant isolation inherited — one schoolId fans out to all withSchool-scoped calls, cross-tenant structurally impossible; PII-minimised arms; server-only; no new surface — read-only library fn, no prod-paste owed).
- **🔵 Forward-notes for later increments (Dex/Sarah advisory — no change now):** (a) top-level `period` is TERM-shaped — GOV-6 (BECE/WASSCE) + GOV-8/9 (annual census) are YEAR-scoped; derive year from `period.academicYear` or extend the `opts` bag, don't force year-scoped arms through the term `period`. (b) GOV-7 facilities is a once-a-term SNAPSHOT — carry its captured-at INSIDE the arm's `data`, not the live `generatedAt`. (c) enrolment CAPTURED gates on ACTIVE roll>0 (hides lifetime exits for a zero-active school — conscious editorial). (d) keep `gender`/`statusTotals`/`counts` aggregate-only if a source shape ever widens (they're pass-by-reference).
